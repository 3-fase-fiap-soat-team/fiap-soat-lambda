{
  "name": "fiap-soat-lambda",
  "version": "1.0.0",
  "description": "Lambda para autenticação FIAP SOAT",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/node": "^18.15.0",
    "jest": "^29.5.0",
    "typescript": "^5.0.0"
  }
}
